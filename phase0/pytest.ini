[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

markers =
    integration: marks tests that require real external APIs (deselect with -m "not integration")

addopts = -v -m "not integration"
