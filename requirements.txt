# =============================================================================
# AI Advisor Appointment Scheduler — Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# Python version required: 3.11+
# =============================================================================


# -----------------------------------------------------------------------------
# LLM PROVIDERS
# -----------------------------------------------------------------------------
groq>=0.9.0                         # Groq API client (primary LLM — llama-3.1-70b)
anthropic>=0.34.0                   # Anthropic Claude client (fallback LLM)


# -----------------------------------------------------------------------------
# SPEECH — STT (Speech-to-Text)
# -----------------------------------------------------------------------------
google-cloud-speech>=2.26.0         # Google Cloud STT v2 (primary)
deepgram-sdk>=3.5.0                 # Deepgram Nova-2 (STT fallback)
pyaudio>=0.2.14                     # Microphone input / speaker output (local demo)
onnxruntime>=1.18.0                 # Runtime for Silero VAD (Voice Activity Detection)
silero-vad>=5.1                     # Silero VAD model (local, no API cost)


# -----------------------------------------------------------------------------
# SPEECH — TTS (Text-to-Speech)
# -----------------------------------------------------------------------------
google-cloud-texttospeech>=2.16.0   # Google Cloud TTS Neural2 (primary)
pyttsx3>=2.90                       # Local TTS fallback (zero-cost, offline)


# -----------------------------------------------------------------------------
# GOOGLE WORKSPACE APIS (Calendar, Sheets, Gmail)
# -----------------------------------------------------------------------------
google-api-python-client>=2.136.0   # Google Calendar API client
google-auth>=2.31.0                 # Google auth (Service Account)
google-auth-httplib2>=0.2.0         # HTTP adapter for google-auth
google-auth-oauthlib>=1.2.0         # OAuth2 helpers (for dev/testing)
gspread>=6.1.2                      # Google Sheets client (higher-level than raw API)


# -----------------------------------------------------------------------------
# RAG PIPELINE — Vector Search & Embeddings
# -----------------------------------------------------------------------------
chromadb>=0.5.3                     # Local vector database (persistent, no ops)
sentence-transformers>=3.0.1        # Embedding model: all-MiniLM-L6-v2 (local)
langchain-text-splitters>=0.2.2     # Recursive character text splitter for chunking


# -----------------------------------------------------------------------------
# WEB SCRAPING (Phase 0 — FAQ data ingestion)
# -----------------------------------------------------------------------------
beautifulsoup4>=4.12.3              # HTML parsing for FAQ scraping
httpx>=0.27.0                       # Async HTTP client (scraping + external API calls)
lxml>=5.2.2                         # Fast HTML/XML parser backend for BeautifulSoup
playwright>=1.44.0                  # Headless browser for JS-rendered pages (optional)


# -----------------------------------------------------------------------------
# WEB FRAMEWORK — FastAPI Backend
# -----------------------------------------------------------------------------
fastapi>=0.111.0                    # Async REST + WebSocket API framework
uvicorn[standard]>=0.30.1           # ASGI server for FastAPI
websockets>=12.0                    # WebSocket support for real-time audio streaming
python-multipart>=0.0.9             # Form/file upload support in FastAPI


# -----------------------------------------------------------------------------
# DEMO & TESTING UI — Streamlit
# -----------------------------------------------------------------------------
streamlit>=1.36.0                   # Web UI for text-based testing console and voice demo
plotly>=5.18.0                      # Interactive charts for homepage analytics section
streamlit-webrtc>=0.47.6            # Browser microphone/speaker WebRTC component
requests>=2.31.0                    # HTTP client (used by Sarvam AI TTS API)
gTTS>=2.5.0                         # Google TTS fallback (last-resort, always available)


# -----------------------------------------------------------------------------
# TASK QUEUE (async MCP retry worker)
# Development: asyncio (no extra dep). Production: Celery + Redis.
# -----------------------------------------------------------------------------
celery>=5.4.0                       # Distributed task queue (production retry worker)
redis>=5.0.7                        # Redis client (session store + Celery broker)


# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------
itsdangerous>=2.2.0                 # HMAC-signed secure URL token generation
cryptography>=42.0.8                # Underlying crypto primitives for itsdangerous


# -----------------------------------------------------------------------------
# CONFIGURATION & ENVIRONMENT
# -----------------------------------------------------------------------------
pydantic-settings>=2.3.4            # Typed settings loaded from .env (validates on startup)
python-dotenv>=1.0.1                # .env file loader


# -----------------------------------------------------------------------------
# DATE / TIME
# -----------------------------------------------------------------------------
pytz>=2024.1                        # IST timezone handling (Asia/Kolkata)
# Note: zoneinfo is stdlib in Python 3.9+, no separate install needed.


# -----------------------------------------------------------------------------
# STRUCTURED LOGGING
# -----------------------------------------------------------------------------
structlog>=24.2.0                   # Structured JSON logging


# -----------------------------------------------------------------------------
# TOKEN COUNTING (system prompt budget check)
# -----------------------------------------------------------------------------
tiktoken>=0.7.0                     # OpenAI-compatible tokeniser for prompt length checks


# -----------------------------------------------------------------------------
# TESTING
# -----------------------------------------------------------------------------
pytest>=8.2.2                       # Core test runner
pytest-asyncio>=0.23.7              # Async test support (for FastAPI + asyncio code)
pytest-mock>=3.14.0                 # Mock/patch helpers (mocker fixture)
pytest-cov>=5.0.0                   # Coverage reporting
freezegun>=1.5.1                    # Freeze time for testing token expiry (TC-1.11)
httpx>=0.27.0                       # Async test client for FastAPI (also used in app)
respx>=0.21.1                       # Mock httpx requests in tests


# -----------------------------------------------------------------------------
# CODE QUALITY
# -----------------------------------------------------------------------------
ruff>=0.4.9                         # Fast linter + formatter (replaces flake8/black/isort)
pre-commit>=3.7.1                   # Git hook runner for lint/format checks


# -----------------------------------------------------------------------------
# OPTIONAL — Development Convenience
# -----------------------------------------------------------------------------
ipython>=8.25.0                     # REPL for interactive debugging
rich>=13.7.1                        # Pretty terminal output for scripts
