[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that hit real external APIs
    slow: marks tests that are slow (concurrent load)

addopts = -v -m "not integration"
