[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that hit real Google APIs (deselect with -m "not integration")

addopts = -v -m "not integration"
