[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that need real LLM API keys (deselect with -m "not integration")

addopts = -v -m "not integration"
